import PasscodeLock from "./components/PasscodeLock";

export default function App() {
  return (
    <div style={{ background: "#000", minHeight: "100dvh", minWidth: "100vw" }}>
      <PasscodeLock />
    </div>
  );
}
